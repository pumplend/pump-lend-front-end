/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  trailingSlash: true,
  ssr: false,
  reactStrictMode: true,
  // eslint: {
  //   ignoreDuringBuilds: true,
  // },
}
